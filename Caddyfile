# Caddy obtains and renews a Let's Encrypt certificate for DOMAIN automatically and forwards
# everything to the API container, which also serves the built frontend.
{$DOMAIN} {
	encode zstd gzip
	reverse_proxy api:4000
}
