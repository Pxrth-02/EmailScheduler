# Copy to .env. Never commit the real file.

# Backend origin. In development the Vite dev server proxies /api, /auth and /health here,
# so the app itself talks to its own origin and cookies just work.
VITE_API_URL=http://localhost:4000
